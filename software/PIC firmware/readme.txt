usage is simple :
python ./PTL-ino-update_serial.py -i ptl-ino.PTL-000.hex -s 042 -o ptl-ino.PTL-042.hex

this will create firmware with serial number 042 in hexadecimal. You have to provide 3 digits for the serial number (pad with 0 if needed)

